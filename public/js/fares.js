// ==============================
// NextMetro — Fare Calculator Page JS
// Depends on shared.js (loaded first)
// ==============================

// Codes to skip (multi-platform duplicates / S-prefix duplicates)
const skipCodes = ['C01', 'F01', 'F03', 'E06', 'S04', 'S09', 'S10', 'S12', 'S13', 'S14'];
/* nm:links:station-routes */
// Generated by tools/gen-fares-links.js from public/data/stations.json — do not edit by hand.
var stationRoutes = {
  A01: { slug: 'metro-center', lines: ['red','orange','blue','silver'] },
  A02: { slug: 'farragut-north', lines: ['red'] },
  A03: { slug: 'dupont-circle', lines: ['red'] },
  A04: { slug: 'woodley-park', lines: ['red'] },
  A05: { slug: 'cleveland-park', lines: ['red'] },
  A06: { slug: 'van-ness-udc', lines: ['red'] },
  A07: { slug: 'tenleytown-au', lines: ['red'] },
  A08: { slug: 'friendship-heights', lines: ['red'] },
  A09: { slug: 'bethesda', lines: ['red'] },
  A10: { slug: 'medical-center', lines: ['red'] },
  A11: { slug: 'grosvenor-strathmore', lines: ['red'] },
  A12: { slug: 'north-bethesda', lines: ['red'] },
  A13: { slug: 'twinbrook', lines: ['red'] },
  A14: { slug: 'rockville', lines: ['red'] },
  A15: { slug: 'shady-grove', lines: ['red'] },
  B01: { slug: 'gallery-place', lines: ['red','yellow','green'] },
  B02: { slug: 'judiciary-square', lines: ['red'] },
  B03: { slug: 'union-station', lines: ['red'] },
  B04: { slug: 'rhode-island-ave', lines: ['red'] },
  B05: { slug: 'brookland-cua', lines: ['red'] },
  B06: { slug: 'fort-totten', lines: ['red','yellow','green'] },
  B07: { slug: 'takoma', lines: ['red'] },
  B08: { slug: 'silver-spring', lines: ['red'] },
  B09: { slug: 'forest-glen', lines: ['red'] },
  B10: { slug: 'wheaton', lines: ['red'] },
  B11: { slug: 'glenmont', lines: ['red'] },
  B35: { slug: 'noma', lines: ['red'] },
  C02: { slug: 'mcpherson-square', lines: ['orange','blue','silver'] },
  C03: { slug: 'farragut-west', lines: ['orange','blue','silver'] },
  C04: { slug: 'foggy-bottom', lines: ['orange','blue','silver'] },
  C05: { slug: 'rosslyn', lines: ['orange','blue','silver'] },
  C06: { slug: 'arlington-cemetery', lines: ['blue'] },
  C07: { slug: 'pentagon', lines: ['blue','yellow'] },
  C08: { slug: 'pentagon-city', lines: ['blue','yellow'] },
  C09: { slug: 'crystal-city', lines: ['blue','yellow'] },
  C10: { slug: 'dca-national-airport', lines: ['blue','yellow'] },
  C11: { slug: 'potomac-yard', lines: ['blue','yellow'] },
  C12: { slug: 'braddock-road', lines: ['blue','yellow'] },
  C13: { slug: 'king-street', lines: ['blue','yellow'] },
  C14: { slug: 'eisenhower-ave', lines: ['yellow'] },
  C15: { slug: 'huntington', lines: ['yellow'] },
  D01: { slug: 'federal-triangle', lines: ['orange','blue','silver'] },
  D02: { slug: 'smithsonian', lines: ['orange','blue','silver'] },
  D03: { slug: 'lenfant-plaza', lines: ['orange','blue','yellow','green','silver'] },
  D04: { slug: 'federal-center-sw', lines: ['orange','blue','silver'] },
  D05: { slug: 'capitol-south', lines: ['orange','blue','silver'] },
  D06: { slug: 'eastern-market', lines: ['orange','blue','silver'] },
  D07: { slug: 'potomac-ave', lines: ['orange','blue','silver'] },
  D08: { slug: 'stadium-armory', lines: ['orange','blue','silver'] },
  D09: { slug: 'minnesota-ave', lines: ['orange'] },
  D10: { slug: 'deanwood', lines: ['orange'] },
  D11: { slug: 'cheverly', lines: ['orange'] },
  D12: { slug: 'landover', lines: ['orange'] },
  D13: { slug: 'new-carrollton', lines: ['orange'] },
  E01: { slug: 'mt-vernon-sq', lines: ['yellow','green'] },
  E02: { slug: 'shaw-howard-u', lines: ['yellow','green'] },
  E03: { slug: 'u-street', lines: ['yellow','green'] },
  E04: { slug: 'columbia-heights', lines: ['yellow','green'] },
  E05: { slug: 'georgia-ave-petworth', lines: ['yellow','green'] },
  E07: { slug: 'west-hyattsville', lines: ['yellow','green'] },
  E08: { slug: 'hyattsville-crossing', lines: ['yellow','green'] },
  E09: { slug: 'college-park', lines: ['yellow','green'] },
  E10: { slug: 'greenbelt', lines: ['yellow','green'] },
  F02: { slug: 'archives', lines: ['yellow','green'] },
  F04: { slug: 'waterfront', lines: ['green'] },
  F05: { slug: 'navy-yard-ballpark', lines: ['green'] },
  F06: { slug: 'anacostia', lines: ['green'] },
  F07: { slug: 'congress-heights', lines: ['green'] },
  F08: { slug: 'southern-ave', lines: ['green'] },
  F09: { slug: 'naylor-road', lines: ['green'] },
  F10: { slug: 'suitland', lines: ['green'] },
  F11: { slug: 'branch-ave', lines: ['green'] },
  G01: { slug: 'benning-road', lines: ['blue','silver'] },
  G02: { slug: 'capitol-heights', lines: ['blue','silver'] },
  G03: { slug: 'addison-road', lines: ['blue','silver'] },
  G04: { slug: 'morgan-boulevard', lines: ['blue','silver'] },
  G05: { slug: 'downtown-largo', lines: ['blue','silver'] },
  J02: { slug: 'van-dorn-street', lines: ['blue'] },
  J03: { slug: 'franconia-springfield', lines: ['blue'] },
  K01: { slug: 'court-house', lines: ['orange','silver'] },
  K02: { slug: 'clarendon', lines: ['orange','silver'] },
  K03: { slug: 'virginia-square', lines: ['orange','silver'] },
  K04: { slug: 'ballston', lines: ['orange','silver'] },
  K05: { slug: 'east-falls-church', lines: ['orange','silver'] },
  K06: { slug: 'west-falls-church', lines: ['orange'] },
  K07: { slug: 'dunn-loring', lines: ['orange'] },
  K08: { slug: 'vienna', lines: ['orange'] },
  N01: { slug: 'mclean', lines: ['silver'] },
  N02: { slug: 'tysons', lines: ['silver'] },
  N03: { slug: 'greensboro', lines: ['silver'] },
  N04: { slug: 'spring-hill', lines: ['silver'] },
  N06: { slug: 'wiehle-reston-east', lines: ['silver'] },
  N07: { slug: 'reston-town-center', lines: ['silver'] },
  N08: { slug: 'herndon', lines: ['silver'] },
  N09: { slug: 'innovation-center', lines: ['silver'] },
  N10: { slug: 'washington-dulles', lines: ['silver'] },
  N11: { slug: 'loudoun-gateway', lines: ['silver'] },
  N12: { slug: 'ashburn', lines: ['silver'] },
  // multi-platform partner codes
  C01: { slug: 'metro-center', lines: ['red','orange','blue','silver'] },
  F01: { slug: 'gallery-place', lines: ['red','yellow','green'] },
  E06: { slug: 'fort-totten', lines: ['red','yellow','green'] },
  F03: { slug: 'lenfant-plaza', lines: ['orange','blue','yellow','green','silver'] },
};

var lineDisplayNames = {
  red: 'Red Line',
  orange: 'Orange Line',
  blue: 'Blue Line',
  green: 'Green Line',
  yellow: 'Yellow Line',
  silver: 'Silver Line',
};
/* /nm:links:station-routes */

// ---- State ----
let currentFareData = null;

// ---- DOM Elements ----
const calcFrom = document.getElementById('calc-from');
const calcTo = document.getElementById('calc-to');
const calcSwap = document.getElementById('calc-swap');
const calcResults = document.getElementById('calc-results');
const resultPeak = document.getElementById('result-peak');
const resultOffpeak = document.getElementById('result-offpeak');
const resultTime = document.getElementById('result-time');
const resultRoundtrip = document.getElementById('result-roundtrip');
const resultRoundtripNote = document.getElementById('result-roundtrip-note');
const tripsPerWeek = document.getElementById('trips-per-week');
const estWeekly = document.getElementById('est-weekly');
const estMonthly = document.getElementById('est-monthly');
const estYearly = document.getElementById('est-yearly');
const peakDot = document.getElementById('peak-dot');
const peakText = document.getElementById('peak-text');
const peakTimeEl = document.getElementById('peak-time');
const resultSenior = document.getElementById('result-senior');
const resultSeniorAlt = document.getElementById('result-senior-alt');
const boardRowWeekday = document.getElementById('board-row-weekday');
const boardRowLatenight = document.getElementById('board-row-latenight');
const boardNowWeekday = document.getElementById('board-now-weekday');
const boardNowLatenight = document.getElementById('board-now-latenight');
const faresTrip = document.getElementById('fares-trip');
const faresTripStations = document.getElementById('fares-trip-stations');
const faresTripSegment = document.getElementById('fares-trip-segment');

// ==============================
// Weekday / Late-Night-Weekend Fare Detection
// WMATA eliminated peak/off-peak within weekdays in June 2023.
// The fare distinction is now: weekday (opening–9:30 PM) vs.
// late night (after 9:30 PM) / weekends / federal holidays.
// ==============================
function isWeekdayFare(date) {
  const day = date.getDay(); // 0 = Sunday, 6 = Saturday
  if (day === 0 || day === 6) return false; // weekends

  const hours = date.getHours();
  const minutes = date.getMinutes();
  const timeInMinutes = hours * 60 + minutes;

  // Weekday fares apply from opening (~5:00 AM) until 9:30 PM
  const openingTime = 5 * 60;       // 5:00 AM
  const lateNightStart = 21 * 60 + 30; // 9:30 PM

  return timeInMinutes >= openingTime && timeInMinutes < lateNightStart;
}

// Keep isPeakTime as an alias — the WMATA API still returns
// PeakTime / OffPeakTime fields used by the calculator.
const isPeakTime = isWeekdayFare;

function updatePeakIndicator() {
  const now = new Date();
  const weekday = isWeekdayFare(now);
  const day = now.getDay();
  const isWeekend = day === 0 || day === 6;

  if (weekday) {
    peakDot.className = 'peak-indicator-dot peak-indicator-dot--peak';
    peakText.textContent = 'Weekday Fares Active';
  } else if (isWeekend) {
    peakDot.className = 'peak-indicator-dot peak-indicator-dot--offpeak';
    peakText.textContent = 'Weekend Fares Active';
  } else {
    peakDot.className = 'peak-indicator-dot peak-indicator-dot--offpeak';
    peakText.textContent = 'Late Night Fares Active';
  }

  peakTimeEl.textContent = now.toLocaleTimeString([], {
    hour: 'numeric',
    minute: '2-digit',
    weekday: 'short',
  });
}

// ==============================
// Reduced Fare Calculation
// WMATA API SeniorDisabled field is stale for routes over ~4.5 miles.
// Compute client-side as 50% of regular fare, rounded to nearest $0.05,
// with a $1.10 floor — matches WMATA's published reduced fare policy.
// ==============================
function reducedFare(amount) {
  var cents = Math.round(amount * 100);
  var halfCents = Math.floor(cents / 2);
  var rounded = Math.floor(halfCents / 5) * 5; // floor to nearest 5¢
  return Math.max(1.10, rounded / 100);
}

// ==============================
// Populate Station Selectors
// ==============================
function populateStations() {
  const seen = new Set();
  const opts = [];

  Object.entries(stations).forEach(([code, name]) => {
    if (skipCodes.includes(code)) return;
    if (seen.has(name)) return;
    seen.add(name);
    opts.push({ code, name });
  });

  opts.sort((a, b) => a.name.localeCompare(b.name));

  [calcFrom, calcTo].forEach((select) => {
    const defaultOpt = select.querySelector('option');
    select.innerHTML = '';
    select.appendChild(defaultOpt);

    opts.forEach((opt) => {
      const option = document.createElement('option');
      option.value = opt.code;
      option.textContent = opt.name;
      select.appendChild(option);
    });
  });
}

// ==============================
// Fetch & Display Fare
// ==============================
async function fetchFare() {
  const fromCode = calcFrom.value;
  const toCode = calcTo.value;

  renderTripBlock();

  if (!fromCode || !toCode) {
    calcResults.style.display = 'none';
    currentFareData = null;
    return;
  }

  if (fromCode === toCode) {
    currentFareData = { peak: 0, offpeak: 0, seniorPeak: 0, seniorOffpeak: 0, time: 0 };
    displayResults();
    calcResults.style.display = '';
    return;
  }

  try {
    const res = await fetchWithRetry(
      API_BASE_URL + '/api/fare/' + fromCode + '/' + toCode
    );
    if (!res.ok) throw new Error('Fare API error');
    const data = await safeJson(res);

    const info = data.StationToStationInfos && data.StationToStationInfos[0];
    if (!info) {
      calcResults.style.display = 'none';
      currentFareData = null;
      return;
    }

    const fare = info.RailFare || {};
    const peakAmt = fare.PeakTime || 0;
    const offpeakAmt = fare.OffPeakTime || 0;
    currentFareData = {
      peak: peakAmt,
      offpeak: offpeakAmt,
      seniorPeak: reducedFare(peakAmt),
      seniorOffpeak: reducedFare(offpeakAmt),
      time: info.RailTime || 0,
    };

    displayResults();
    calcResults.style.display = '';
  } catch (err) {
    console.error('Fare fetch error:', err.message);
    resultPeak.textContent = '--';
    resultOffpeak.textContent = '--';
    resultSenior.textContent = '--';
    resultSeniorAlt.textContent = '';
    resultTime.textContent = 'Unavailable';
    resultRoundtrip.textContent = '--';
    resultRoundtripNote.textContent = '';
    currentFareData = null;
    calcResults.style.display = '';
  }
}

function displayResults() {
  if (!currentFareData) return;

  const d = currentFareData;
  const weekday = isWeekdayFare(new Date());

  resultPeak.textContent = '$' + d.peak.toFixed(2);
  resultOffpeak.textContent = '$' + d.offpeak.toFixed(2);
  resultSenior.textContent = '$' + d.seniorPeak.toFixed(2);
  resultSeniorAlt.textContent = '\u00b7 $' + d.seniorOffpeak.toFixed(2) + ' late';

  // The NOW marker tracks the active fare window.
  boardRowWeekday.classList.toggle('calc-board-row--now', weekday);
  boardRowLatenight.classList.toggle('calc-board-row--now', !weekday);
  boardNowWeekday.hidden = !weekday;
  boardNowLatenight.hidden = weekday;

  const active = weekday ? d.peak : d.offpeak;
  resultRoundtrip.textContent = '$' + (active * 2).toFixed(2);
  resultRoundtripNote.textContent = weekday ? 'Weekday round trip' : 'Late night / weekend round trip';

  resultTime.textContent = d.time ? d.time + ' min' : '--';
  updateEstimator();
}

// ==============================
// This Trip
// The trip's two stations and the lines serving each, as a journey ribbon on
// the page surface below the calculator. Real links, styled like links are
// everywhere else on the site.
// ==============================
function escapeHtml(str) {
  return String(str)
    .replace(/&/g, '&amp;')
    .replace(/</g, '&lt;')
    .replace(/>/g, '&gt;')
    .replace(/"/g, '&quot;');
}

function tripStationEntry(code) {
  const route = stationRoutes[code];
  const name = stations[code] || '';
  if (!route || !name) return '';
  const chips = route.lines
    .map(
      (line) =>
        '<a href="/lines/' + line + '/" class="fares-line-chip fares-line-chip--' + line + '">' +
        lineDisplayNames[line] +
        '</a>'
    )
    .join('');
  return (
    '<div class="fares-trip-station">' +
    '<a href="/station/' + route.slug + '/" class="fares-trip-station-link">' + escapeHtml(name) + ' station</a>' +
    '<span class="fares-trip-station-lines">' + chips + '</span>' +
    '</div>'
  );
}

function renderTripBlock() {
  if (!faresTrip) return;
  const fromCode = calcFrom.value;
  const toCode = calcTo.value;
  const fromRoute = stationRoutes[fromCode];

  if (!fromCode || !toCode || fromCode === toCode || !fromRoute || !stationRoutes[toCode]) {
    faresTripStations.innerHTML = '';
    faresTrip.hidden = true;
    return;
  }

  faresTripStations.innerHTML = tripStationEntry(fromCode) + tripStationEntry(toCode);
  // The ribbon segment carries the colour of the line you board.
  faresTripSegment.className = 'fares-trip-segment fares-trip-segment--' + fromRoute.lines[0];
  faresTrip.hidden = false;
}

// ==============================
// Cost Estimator
// ==============================
function updateEstimator() {
  if (!currentFareData) {
    estWeekly.textContent = '--';
    estMonthly.textContent = '--';
    estYearly.textContent = '--';
    return;
  }

  const trips = parseInt(tripsPerWeek.value, 10) || 0;
  if (trips <= 0) {
    estWeekly.textContent = '--';
    estMonthly.textContent = '--';
    estYearly.textContent = '--';
    return;
  }

  const d = currentFareData;
  // Assume a commuter mix: roughly 60% weekday, 40% late night / weekend.
  const costPerTrip = d.peak * 0.6 + d.offpeak * 0.4;

  // Each "round trip" is 2 one-way trips
  const weekly = costPerTrip * trips * 2;
  const monthly = weekly * 4.33; // average weeks per month
  const yearly = weekly * 52;

  estWeekly.textContent = '$' + weekly.toFixed(2);
  estMonthly.textContent = '$' + monthly.toFixed(2);
  estYearly.textContent = '$' + yearly.toFixed(2);
}

// ==============================
// Swap Stations
// ==============================
calcSwap.addEventListener('click', () => {
  const fromVal = calcFrom.value;
  const toVal = calcTo.value;
  calcFrom.value = toVal;
  calcTo.value = fromVal;
  fetchFare();
});

// ==============================
// Event Listeners
// ==============================
calcFrom.addEventListener('change', fetchFare);
calcTo.addEventListener('change', fetchFare);
tripsPerWeek.addEventListener('input', updateEstimator);

// ==============================
// Init
// ==============================
document.addEventListener('DOMContentLoaded', async () => {
  populateStations();
  updatePeakIndicator();

  // Update the hero indicator and the board's NOW marker every minute, so
  // crossing the 9:30 PM boundary while the page is open moves both.
  setInterval(function () {
    updatePeakIndicator();
    displayResults();
  }, 60000);

  // Wake up Render backend
  try {
    await fetchWithRetry(API_BASE_URL + '/healthz', 3, 2000);
  } catch (e) {
    // Backend may be down — fare fetches will handle gracefully
  }
});
